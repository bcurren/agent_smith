1. clone the repo
2. git submodule update --init --recursive
3. sudo mkdir -p /usr/local/share/man/man.1
4. mkdir ~/.node_libraries
5. cd deps/express && sudo make install-support
6. install spark by cloning http://github.com/senchalabs/spark.git
7. start the event_server by cd event_server; spark -p 8000

# Not yet:)
# 1. Install npm: curl http://npmjs.org/install.sh | sudo sh
# 2. Install submodules: sh config/install_modules.sh
